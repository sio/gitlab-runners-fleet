all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    packages_recommends: false
    server_remote_user: op
  children:
    gateway:
      hosts:
        localhost:
    runner:
      hosts:
        localhost:
  hosts:
    localhost:
      ansible_connection: local
