# /etc/gitlab-runners-fleet.env
#
# Assign secret values to environment variables,
# see `makefiles/environment.mk` for the list of required variables.

GITLAB_API_TOKEN="GitLab API token with read-write access"
GITLAB_RUNNER_TOKEN="GitLab runner registration token"
HCLOUD_TOKEN="Hetzner Cloud API token"
PULUMI_CONFIG_PASSPHRASE="Passphrase to unlock Pulumi config/secrets, can be anything when deploying a new stack"
